# Hosts that install with npm (e.g. Hostinger) crash on this dependency tree's
# peer-dep resolution (npm arborist 'edgesOut' bug). pnpm is the source of truth
# locally (pnpm-lock.yaml); this only affects npm installs.
legacy-peer-deps=true
